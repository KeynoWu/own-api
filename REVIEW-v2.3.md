# auto 路由 v2.3 研发工作多维审查报告

- **审查对象**：`4cb5360..HEAD`（5 提交：6a81c8f P0 / efbba6d P1 / f2f6c94 P1.5 / 1fce94f P2 / 0bc1431 文档）
- **审查方式**：7 个专项维度并行深读（设计符合性 / 数学与状态机 / 并发与生命周期 / 安全 / 测试质量 / 性能 / 对抗性不变量猎手），全部 high/medium 发现再经独立复核员逐条对码裁决
- **结论速览**：总体符合度高，**无 high 级问题**；确认 medium 7 条、low 一批；性能维度全量判「可忽略」，无伪优化必要。安全问题仅 2 条 low（其一为基线期存量）。测试基线（295 e2e + 184 hardening）经复核实测全绿。
- **复核裁决**：11 条 high/medium 抽检 → 9 confirmed / 1 partial（结论成立、机制归属修正）/ 1 降级为 low。无 refuted。

---

## 一、速度因子状态机的四个真缺陷（同一处读侧表达式，建议一并修）

审查团在 `speedFactorOf` 的读侧路径（`src/auto.ts:455-461`）交叉发现了四个相互独立、可一次修复的问题。四条均经代码仿真/实测复现确认：

| # | 问题 | 复核 | 影响 |
|---|------|------|------|
| 1 | **stale EMA 绕过 fresh<3 冷启动门**：读侧 factor 对 `speedEma` 无样本新鲜度门槛，而 bench 门（auto.ts:449）与 EMA 更新门（:404）都有；且 `speedNote` 每个新样本重置 `lastSampleAt` | confirmed (A7) | 涓流流量（采样间隔 >1h，低流量慢候选常态）下 factor 被无限期钉在历史值——既不更新也不衰减，与「无任何合格候选 → factor=1」的自家注释矛盾 |
| 2 | **decode 窗 <1ms 毒样本**：`Math.max(0.001,…)` 下界把亚毫秒 decode 窗记为 ct×1000 tok/s | 复核实测：起真实网关 tokP50=**17000**（mock 同步 enqueue 命中此路径） | agent 短回复（17-50 tok 整段一个 TCP 包）即触发；若毒候选成为 bench，诚实候选被压到 floor，流量比可倒挂 4:1 |
| 3 | **EMA 锯齿**：事件侧混合用未按时间衰减的存量值（α·raw + (1-α)·prev），衰减只在读侧发生 | 复核实测：停 5h 读 1.03 → 单个新样本瞬间跳回 2.0 | F3.1 要消灭的振荡以锯齿形式回归；与 F3.4「无硬跳变」动机相悖 |
| 4 | **读侧无当前 cfg 重钳 + 负 dt 无钳**：floor/cap PATCH 后存量 EMA 越出新带（实测 ema=1.3 + cap=1.2 → 读 1.3）；墙钟回拨 Δ 使 `pow(0.5, 负数)` 指数爆炸（实测回拨 6h → factor 65） | confirmed（A7-b 降 low；回拨条 low） | 回滚预案依赖的热改配置对存量状态无效；Tauri 桌面壳休眠恢复/NTP 校正是现实触发面 |

**建议修法（一处收口）**：读侧重算改为
`factor = clamp(1 + (ema-1) × pow(0.5, max(0, now-last)/1h), floor, cap)`，且 `fresh < 3` 时直接取 1（与 bench 门同口径）。一行公式同时修掉 #1/#3 的读侧半边/#4；#2 在 `speedNote` 入口加 `decode 窗 <50ms 丢样`（或 tokPerSec 物理上限）；#3 的写侧半边改为事件侧先衰减存量再混合。配套：`speedNote` 入口加 `cfg.enabled` 早退（P6 顺手项，关闸时连采样成本都省掉）。

## 二、生命周期缺口（多维度交叉确认， medium 降 low）

5. **删除钩子只修了一半**：`clearSpeedForRoute` 全仓零调用（死代码），DELETE /routes、DELETE /channels 级联只清健康窗+粘性；饱和/速度五态/视觉学习记忆（含图片 SHA-256 指纹，数据最小化缺口）全部残留至重启。已删路由的 ghost 样本**继续进 bench**（观测面不可见却毒化观测值，实测扭曲在跑候选相对权重；1h 窗自愈）。同时存在**流末悬空写入**：提交后的流在管理员删路由后才结束，会把已清的窗口复活成幽灵观测行（/tmp 实测复现）。→ 修法：删除链补 `clearSpeedForRoute + clearSaturationForRoute + clearVisionLearning`（钩子全部已实现，只差接线），或收敛为一个 `clearRouteStateFor(id)` 聚合钩子；`finished()` 采样前加一次存活校验。
6. **vkey 删除不回收**：DELETE /vkeys/:id 只 forgetQuota，sticky 残留占 1000 槽 LRU 名额、lastFailAt 按 (route×vkey) 对累积无帽（20 路由×500 员工 ≈ 1MB 量级，无爆点但无回收）。

## 三、设计符合性（3 medium + 文档漂移组）

7. **autoVision.enabled 语义不完整**（R9「三信号+视觉各有 enabled 开关可回滚」走样）：enabled 只闸学习闭环（gateway.ts:414）；`supportsVision===false` 的软排除（:221）、unknown 的 0.25 降权（:1035）、inputEst 图片预占都不受闸。事故回滚时视觉逻辑名存实亡。→ 三处补闸，或在 §6 明确声明 enabled 仅闸学习。
8. **http 图片 token 常数 1000 未按 §10 裁决改 ≥1600**：`gateway.ts:1271 ?? 1000` 与模块头注释「≈1600/张」自相矛盾，且 VIS-5 双钉把 1000 锁死。带 http 图请求的限额预占被反向低估（恰是 R7 要修的方向）。
9. **§6 验收基建缺位**：三个验收指标（跨候选失败率/全链失败率/p95 TTFT）与 G21②③（口径改 chainAttempts>1、from/to 参数）未实现，usage.ts 在本区间零改动——「降幅 ≥30% 否则回滚」的判据目前无法执行。
10. **文档-实现漂移**（建议归入 §9 第三批已钉偏差）：饱和公式 doc `2^n` vs code `2^(n-1)`、G11「n 照常递增」vs 同 episode 不递增、F3.4 衰减起点是最后样本时刻而非滚出窗时刻、快照 survivors 缺 sampleCount/visionBias、excluded kind 收窄为 hard/soft 且 e.ok=true 的饱和候选不进 excluded、链内 speedFactor 逐跳现读（health 已冻结）。
11. **新设置无管理台入口**：三组开关只能裸调 PATCH /api/settings，设置页不可见。

## 四、测试缺口（下个迭代的钉单）

12. **速度因子端到端接线零验证**（最重要的一条测试缺口）：e2e 域内 factor 恒等于 1（单候选 bench=自身），SPD-2/R8 与 F5.2 的 factor 断言全是 `typeof === 'number'` 恒真型——`speedFactorOf` 恒返 1 或 `ewOf` 漏乘都全绿。hardening 有精确值钉（2.0/0.5）但不经 gateway 接线。→ 补双候选高/低吞吐钉，让 factor≠1 真正进入 ew 断言。
13. **三条负向行为零钉**：direct 不采样、非流式不采样、G15 的 15/16 token 准入线（mock 流式 usage 恒 17，边界不可达）——三类「毒样本静默入权重层」的回归均无护栏。
14. **G14/G15 实现正确但零覆盖**：把 `attemptT0` 换回链级 t0、删掉 `ct>=16` 全部测试仍绿。
15. **SPD-3 e2e 精确卡点**：置位判据实际是 s/f≥3.5（复核修正：稳住界由 2× 回粘侧推出，非 3× 支）、「恰好 6 个慢样本」零容差（第 5 个置位、第 7 个翻回）。当前余量 8-50 倍不 flake，但对测试代码改动极脆；两处注释失真（「第 6 个起转慢」实为第 5 个；SAT 探测假时钟「剩 ~3s」实为 ~0.9s）。
16. 其他零钉：speedBench 冷库 null、cap=1 合法退化、新设置校验分支（sanitizeSettings 三组）、G14/G15 负例。

## 五、安全（面总体受控，2 low）

- 鉴权全覆盖、观测字段无敏感泄漏（stickyList 仅 vk_ 随机 id）、settings 校验实测无绕过、控制台渲染零 innerHTML 旁路、学习闭环四道防投毒闸扎实——以上为复核确认的正面结论。
- low×2：路由删除后图片指纹残留且无清理入口（并入 #5 的聚合钩子）；404 空存活 detail 的未授权候选名掩码被检查顺序旁路（**基线期已存在**，非本区间引入，建议结构化 authorized 标志替代 reason 文案匹配）。
- mock ttft(N) 正则：仅 127.0.0.1 手动启动的测试装置，生产不可利用（info）。

## 六、性能（全部【可忽略】，无需优化）

每请求热路径新增 ~2-5µs（比既有 healthOf 便宜一个数量级）；事件侧重算 ~100µs/次 × 流末频率 ≤1ms CPU/s；enrichRoute 新增查表是 O(1) Map（优于区间前既有的 snap.find O(n²) 模式）；429 风暴三条清除路径闭环。仅两项「可观测·低」：七态删除残留（年度 KB~MB，即 #5）与 pickSnapshot 日志 +731B/条（logRetention 调到 20 万上限时才值得留意）。四个候选「优化」经量级核算均为伪优化。

---

## 值得肯定（复核确认）

- F1.1 续期 scope 三分完整落地且可断言；G1 合成 429 独立构造绕过 retryAfterHeader 钳制（SAT-2 直接断言响应头）
- R4 权重单点实现（ewOf 同时服务首跳/续链/快照）+ pickWeighted NaN→0 兜底 + NAN-1 直测
- §8 钉表 29 枚钉全部真实存在、断言实质到位（CHN-1/CHN-2 以 AR-7/F5.2 名义落钉）；G14/G15/G16/G17/F6.3/§6 校验边界六项特别核对全部对版
- 安全面：管理台零 XSS 旁路、观测字段干净、指纹不出进程、限频按 (routeId,vkey) 封顶
- 时钟注入纪律（成对恢复）与 sticky 的「删除即清 + 悬空自愈」双保险
- 性能设计：读侧 5s 缓存 + 事件侧驱动分离正确，四项伪优化被排除

## 建议的行动顺序

1. **一个快修提交**（速度因子读侧统一公式 + 毒样本丢样 + 删除钩子接线 + 流末存活校验 + enabled 早退）——#1-#5 全部收口，改动集中在 auto.ts/admin.ts/gateway.ts 约 30 行
2. **一个测试补强提交**：#12 双候选因子断言、#13 三条负向钉、#15 注释修正
3. **文档对齐提交**：#10 漂移组归入 §9、#8 裁决落实（或声明保留理由）
4. 排期项：autoVision.enabled 完整闸、验收三件套、设置页入口

*审查方法：9+3 个并行代理，七维（设计符合性 D1 / 数学 M2 / 生命周期 L3 / 安全 S4 / 测试 T5 / 性能 P6 / 对抗猎手 A7）+ 双复核员对全部 high/medium 发现逐条对码裁决；关键发现均经 /tmp 仿真或真实网关实测复现，非纸面推断。*
