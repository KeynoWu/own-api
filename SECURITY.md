# 安全策略（Security Policy）

## 报告漏洞

请**不要**在公开 Issue 中披露安全问题。使用 GitHub 私有漏洞报告：
[Security Advisories → Report a vulnerability](../../security/advisories/new)。
通常 72 小时内响应；确认修复前报告内容保持私有。

## 版本支持

| 版本 | 支持 |
| ---- | ---- |
| latest（Releases 页当前版） | ✅ |
| 其余 | ❌ 请升级后复现 |

## 部署须知（安全边界依赖这些前提）

- 网关默认只监听 `127.0.0.1`。改成 `OWN_API_HOST=0.0.0.0` 会把管理与数据面暴露到局域网，请自行确认网络可信。
- 管理令牌（`admin-…`，144 bit 随机）与对外 key（`sk-lm-…`，144 bit）首次启动生成于 `~/.own-api/db.json`（`0600`）——该文件等同密码本，备份时同样对待。
- 上游渠道 baseUrl 是管理员配置面：仅添加你信任的上游。网关不跟随上游 3xx（SSRF 闸），但仍建议对来路不明的转售渠道保持警惕。
- 控制台是单用户设计：拿到管理令牌 = 完全控制。令牌经 URL 注入仅走 60 秒一次性票据，不落地 URL。

## 已内建的安全机制（节选）

管理面鉴权失败限速（爆破 30 连即锁）、回环判定以 socket 对端为准（伪造 Host/XFF 无效）、密钥全链路掩码、配置组导出零密钥、请求体/帧大小硬上限、db.json 原子写 + 0600、CORS 默认仅环回来源。

一键接入 Agent（写本机 agent 配置）额外四道：写入面**零路径入参**（只认适配器表里的 `agentId`，路径穿越在协议层不可表示）、**回环硬闸**（写入/撤销/探针仅限 `127.0.0.1` 直连，与明文 key 的可见范围同闸）、**merge-only**（只操作声明的托管路径，解析失败一律拒写不猜，不跟随符号链接，写前自动备份并在写后回读校验）、探针**不构成 SSRF 面**（目标 URL 恒取本进程自己的监听地址，请求体里没有 URL 字段）。

YAML 适配器（omp）再加三道：**角色槽只认适配器声明过的名字**——槽名会成为目标文件里的路径段，请求里传 `../evil` 之类的名字在入口即被丢弃（零路径入参纪律的延伸，钉 AI-36）；托管路径**途经 YAML 别名（`*anchor`）即拒写**，否则一次写入会改到锚点指向的公共内容，波及用户其它 provider（钉 AI-34）；解析失败的 YAML **拒写不猜**，绝不整文件重写"修好"它（钉 AI-35），序列化强制禁折行以免改写用户无关长行。

裸值型适配器（Claude Code 写 `env.*` 与顶层 `model`，没有 `own-api/` 这种命名空间可作归属标记）再加三道：**角色必须有唯一承接点**（`roleTarget`，planLink 先校验后写；缺失或重复一律拒绝落盘，否则角色名会变成用户配置文件根上的垃圾键，钉 AI-51/AI-60）；**撤销的归属判据是三态**——「用户改走了」可以清账，「key 已删除、值无从比对」则账本必须保留，否则那枚 token 静默留在别人文件里而界面上再无痕迹（钉 AI-56）；同一份文件里的多个写入点各占独立指纹键，避免后写的半个文件把前一半对漂移检测隐去（钉 AI-57）。

未实机验证的适配器（当前是 Claude Code）有一条底线承诺由测试钉死：**真实 `~/.claude/settings.json` 的 mtime 在全测试周期内不变**（钉 AI-0b）——所有写入都发生在 `OWN_API_AGENT_HOME` 指向的沙盒 HOME 里。「实现且钉覆盖」不等于「已验」，其字段形态来自残留配置，行为结论为零。

写入 dsh（`~/.dsh/`）再加两道：凭据明文**只**落 dsh 自己的密码本 `.credentials.yaml` 里 `refs.OWN_API_API_KEY` 那一格（settings 侧只写引用名；`apiKeyEnv` 指向的引用解析不到值时 dsh 以 `MISSING_CREDENTIAL` 明确失败，不会静默改用环境里恰好存在的无关密钥），**且该文件权限必须保持 0600**——dsh 见到多用户可读会拒绝加载整个凭据库，写宽一个 bit 就是让用户自己的 agent 起不来（钉 AI-64）。dsh 的 `settings.yaml` 对未知顶层键是「启动失败」级校验，因此我们只写适配器声明过的三条路径；`DSH_HOME` 指向别处、或凭据库尚未生成，两种前提不满足的情况由 `Adapter.preflight` 一律**拒写**而不是警告后继续（钉 AI-61、AI-61b）。

写前的备份 `<文件>.own-api-bak` **一律收紧到 0600**（钉 AI-69）：它是同一份明文的第二个副本，不该跟着源文件的 0644 走。源文件本身的权限仍按用户自己的选择不动。

AI-0b 哨兵现在同时盯着真实 `~/.claude/settings.json` 与真实 `~/.dsh/` 两份文件的 mtime——**本轮测试就跑在 dsh 里**，写坏它等于自毁，这条是全功能最硬的一条底线。

