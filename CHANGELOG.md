# 更新说明（Changelog）

所有版本的安装包在 [Releases](../../releases) 页下载；每个版本对应一节说明。

## v0.1.12（2026-09-15）

> 设计全文 `docs/agent-import-design.md`（含 PR1 落地映射 §11.1，标明哪些是本轮交付、哪些留给 PR2/P1）

- **一键接入 Agent（PR1：基建 + ZCode）**：管理台「对外 Key」页新增「接入 Agent」——自动识别本机装了哪些 agent，按这把 key 的权限（与 `GET /v1/models` 同一个来源）投影模型清单，写前展示托管块 diff（密钥掩码），确认后写入 `<agent 配置>` 并自动备份 `.own-api-bak`、写后回读校验；「接入方式」页新增已接入清单（漂移状态、探针、撤销）
- **安全边界**：写入面零路径入参（只认适配器 `agentId`）、回环硬闸（写入/撤销/探针仅本机直连，与明文 key 同闸）、merge-only + 解析失败拒写不猜 + 不跟随符号链接、探针目标 URL 恒取本进程监听地址（不构成 SSRF 面）；`confirm` 缺失 428、越权模型 400、路径穿越 400 均已钉
- **漂移指纹只覆盖我方声明字段（带 `*` 通配的字段域）**：实测 ZCode 启动会给我们写的每个模型补 `modalities`、给部分模型补 `reasoning`——整块 hash 会每次启动误报「被改」，字段域则不会；同时对方增删我方 catalog 里的模型仍会被判漂移（那会破坏「选择器里的集合 ≡ 这把 key 能调的集合」）
- **撤销的残留保护**：条目已被改写到不指向本网关时**不删**（防误删用户自建同名条目），且账本保留（`partial:true`）以免孤儿配置在 UI 上隐身；`?force=1` 才允许只清账本
- **`db.json` 升到 version 4**（新增 `agentLinks` 登记，本机绑定态，恒不进配置组导出）；v2/v3 库加载时自动补齐并回写真实版本号
- **探针语义**：L1 只查 `/v1/models`（不打上游），遇 429 判 `rate_limited` 而非失败；L2 需显式 `confirm`，真发一次 `max_tokens=1`。修掉交付时踩到的 UI bug：向导弹了确认框却没把 `confirm` 带给服务端，导致 L2 恒 428 被渲染成「探针未通过」（链路其实是通的）；`428/429/409` 现在与真链路故障分开措辞，并加静态钉 AI-24 防止复发
- **一键接入 Agent（PR2 第一步：YAML 引擎 + omp/oh-my-pi）**：写入层抽成 JSON/YAML 双后端（YAML 走 `yaml` 的 Document API，**只动我们那一块**——注释、锚点、块标量、别人的 provider 全部原样留着），新增 **omp 适配器**：写 `providers.own-api` 到 `~/.omp/agent/models.yml`，并按勾选**逐键合并** `config.yml` 的 `modelRoles`（默认只接管 `default` 槽，其余 8 个不动）。撤销是**逐键还原写前原值**（真机记到 `jyld/glm-5.3-flash:auto` 这种带思考后缀的原值），且只对我们那值仍指向 `own-api/` 的键下手。**真机验收**：临时 HOME + `omp v18.1.20` 真二进制 + 真网关，`omp -p` 走 `modelRoles.default → own-api/model_auto` 返回正常，网关侧日志同步出现该请求；连跑两次启动循环，两个配置文件零改动、别人的 5 个 provider 无一被裁（ZCode 会规范化我方块，omp 不会——这条差异决定了 omp 的「条目消失」是真信号）
- **新增运行时依赖 `yaml`**（DR-AI-F 的让步，此前只有 `hono` + `@hono/node-server`）：只被适配器层使用，网关主链路不碰；打包走 `esbuild bundle`，SEA 单文件与桌面安装包自动包含。**让步已量化**：真机文件往返 `models.yml` 零行差异，`config.yml` 唯一重排是空 flow 序列折叠；序列化强制 `lineWidth: 0`（默认 80 列会把用户无关的长行折开，那是真破坏）
- **同步（sync）闭环**：「已接入」每行新增 **同步** 按钮——漂移、网关换端口、模型清单变旧都一键刷回。它打开的还是同一个向导：**预览 diff → 确认两步一步不省**，只是选项按账本预置并禁用（服务端不按前端走，界面就不许演「假可改」）。新端点 `POST /api/agents/:id/sync` 的上下文**全部由账本重建、请求体只允许 `confirm`**；key 已删除/停用、主模型掉出授权这几种情况是 409 说明下一步，不是硬刷（不替你猜 key，也不替你挑模型）。同步只刷新「同步于」，不刷新「接入于」
- **修掉一条从未执行过的分支**：角色槽解析里的 `if (…) for (…) if (…) X; else if (…)` 写法让 `else if` 挂到了里层 `if`，导致 `{槽: 模型}` 对象形态与「按账本回落」两条路一次都没跑过（UI 一直传数组，数组那条恰好是对的，所以测试全绿）。补钉两条并全库扫了同形状；顺带说明**为什么 UI 也需要真点击测试**：读字面量的静态钉看不见运行期拼出来的请求体，L2 那个 bug 就是这么漏掉的
- **一键接入 Agent（PR2 第三支：Claude Code，`~/.claude/settings.json`）**：写 `env.ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` / 角色键（`ANTHROPIC_MODEL`、`ANTHROPIC_DEFAULT_{HAIKU,SONNET,OPUS}_MODEL`）与顶层 `model`。键集来自本机残留配置的**实测**（是 `AUTH_TOKEN` 不是 `API_KEY`；BASE_URL 不带 `/v1`；顶层 `model` 与 env 里的模型名同值）。同一份文件里两处写入点：`env` 逐键合并 + 文件根的 `model`——那份文件实测还躺着 24 个 `enabledPlugins`、`extraKnownMarketplaces` 和 `statusLine` 里带嵌套转义的 shell 命令，整包重写等于毁环境，钉 AI-51 连那句 shell 的引号都逐字钉住。**明确未实机验证**：Claude Code 已从开发机卸载，本轮真身运行 0 次，字段有结论、行为零结论（§14-V7 保持开放）
- **`haiku` 槽是「不接管就出故障」级**：不填时 Claude Code 拿它自己的默认小模型名发给网关 → 404「未登记模型」，现象看起来像网关坏了。故新增 `Adapter.requiredSlots`（UI 预勾、用户仍可取消）+ 接管成主模型时换一条「后台任务会烧贵的」提示；仍**不替用户猜便宜模型**。顶层 `model` 只在接管了 `default` 时才跟着写，且真要改动时预览里单独一行黄字说明「这改变的是你的默认模型」
- **引擎：同一份文件可以有多个托管写入点**。指纹键法改为「单写入点文件仍用裸路径、多写入点文件带路径」——前者的键法一字不变，才不会让已接入的 ZCode 账本升级即全员误报「被改」。同时新增 `staticKeys`（与角色无关但恒归我们的键）与 `roleTarget`（**角色必须有唯一承接点**，planLink 先校验后写，缺失或重复一律拒绝落盘）
- **修掉两处静默失效**：① 合并型写入点的**聚合指纹域**曾经直接吃 `writtenKeys`，而它在合并型这里是空数组，`fingerprintOf(x, [])` 塌成 `sha256("{}")` 常量——等于用户把 omp 的 `modelRoles.default` 改成别家，漂移永远报「一致」（既有钉测的全是「键整段没了」那条更显眼的路）。② 上面那个 `roleTarget` 缺陷的真实后果：**Claude 曾把 `default`/`haiku` 当根键写进 settings.json**，在用户文件根上留下两个垃圾键；发现方式是撤销回执里一句「这两个键的现值不是本次写入的值」——它们从来不是我们的，是我们自己刚写进去的
- **撤销的归属判据抽成 `Adapter.ownsKey` 并改为三态**：裸值型 agent 没有 `own-api/` 前缀可判，必须自带判据。`false`（用户改走了 → 跳过该键并在回执点名，可以清账）与 `'unknown'`（key 已删除、值无从比对 → 账本必须留着）在旧模型里是同一个值，混起来的后果是那枚 token 静默留在别人文件里而界面上再无痕迹。`catalogInFile:false` 让没有模型清单的 agent 不再误报「模型清单待同步」
- 自测：e2e 395 → **425**、hardening 214 → **216**（新增 30 条 e2e 钉 + 2 条真点击 UI 钉，覆盖 claude 的两写入点、三态归属、`requiredSlots` 预勾与「真实 `~/.claude` 全程零改动」哨兵；两处引擎修复都做过证伪：把聚合指纹域退回 `writtenKeys` 恰好红 `AI-40b`+`AI-53`，删掉 UI 的 `requiredSlots` 分支恰好红那两条新钉）。顺带记一笔：`test/e2e.ts` 里「迟滞松手」那条路由健康度钉是**既有 flaky**（与 agent-import 无关，连跑两次一红一绿），尚未处理

- **dsh 适配器（PR2 第四支，全自动）**：`~/.dsh/settings.yaml` 三个写入点——`llm-pi-ai.providers.own-api` 块、`agent-default-model` 默认模型指针（只在接管「默认模型」时动）、以及 `~/.dsh/.credentials.yaml` 里 `refs.OWN_API_API_KEY` 那一格。key 明文不进 settings.yaml；dsh 监听这两份文件，**写完不用重启**。规格不是猜的：本机正在运行的 v0.1.5-rc.2 的 zod schema + 包内文档，并在真机配置的沙盒副本上彩排过（撤销后凭据库语义零差异）。前提不满足一律拒写：`DSH_HOME` 指向别处、凭据库尚未生成。
- **引擎四件（对既有适配器零行为变化，黄金指纹未动）**：`blockKeys`（一份 `build()` 供多个写入点时切分块内容——不切分会把 key 明文与模型指针当 provider 字段写出去）、`Adapter.preflight`（环境级否决，报错而非警告）、`PlanCtx.prevLink` + noop 写前值记账、块型整块覆盖前的**通用告警**（点名会消失的不托管字段，真机里是用户手写的 `defaultInput` 与逐模型 `input`/`maxTokens`）。
- **修复：撤销会把用户自己设的值掏空**。合并写入点在本轮判 noop 时原先什么都不记，于是「用户本来就指着 own-api → 第一轮就是 noop → 撤销只能删键」，dsh 的 `agent-default-model` 会被留成空映射；真机彩排第一次就踩到。现在 noop 优先继承上一轮账本的写前值、没有则记盘上现值（真写过的写入点仍绝不继承，AI-32 语义不变）。连带修复：`sync` 之后撤销丢失用户最早的原值（值没变就永远 noop，旧账本却被新回执整条替换）。
- **关闭待核实 V1/V2**：dsh profile 没有内联 `apiKey` 字段；`apiKeyEnv` 是凭据引用，且**外部编辑 dsh 配置是官方支持的场景**（两份文件都有 watcher 热发布、settings 侧写持跨进程锁并保留注释与他人 namespace）。原 DR-AI-J「不逆向、标 partial」的前提（第三方黑盒不可核实）不再成立，已重开并改为全自动。新增 V11（热发布未做真机写入实验）、V12（shell 里同名环境变量会遮蔽我们写的 refs；与 dsh 并发写的窗口未加锁）。
- **新争点 §15-5 待裁决**：网关换端口后「同步」会被 §9-4「同 id 不是我们的形态 → 拒覆盖」挡掉，而 UI 恰好把换端口列为同步能修的事。当前保持严格并已由 `AI-62a` 钉住，放松与否请裁决。
- **§15-5 已裁决并落地：网关换端口之后「同步」修得好了**。归属判据 `blockLooksOurs` 多收一个「账本记着的旧基址」，`plan`/`apply`/`sync` 三处同源传入（预览与提交同一把尺）；没有账本时一个字不放松，用户手写的同 id 条目照旧不抢。顺带修掉冲突文案里的 `baseURL=?`——那正是该让用户看见的「它现在指着谁」，旧文案只认 zcode 的 `options.baseURL`，YAML 系一律印问号。
- 测试：新增 `AI-61`…`AI-70b`（dsh 全链路：三写入点、注释与 `records` 邻居、0600 保持、noop、漂移、同步刷回、逐键撤销、DSH_HOME 否决、块覆盖告警）；**AI-0b 哨兵扩到真实 `~/.dsh/` 两份文件**——本轮测试就跑在 dsh 里，写坏等于自毁。顺带修掉一条**假绿**：原 `AI-69` 在测试进程改 `process.env` 再打 HTTP，服务端进程读不到，判据改为进程内直调。自测计数 **e2e 425 → 453**、hardening 216（UI 与适配器无关，未新增）。两条**测试侧假绿**被当场逮住并修掉：改 `process.env` 影响不到服务端进程；undici 吞掉手工设置的 `Host`（模拟换端口时差点让一次 noop 冒充「同步成功」）。
- **安全收紧（全适配器）：`.own-api-bak` 备份一律 0600**。备份是同一份明文的第二个副本，原先跟着源文件的权限走——源文件 0644（zcode 就是）时，明文 key 的副本就躺在世界可读的权限上。源文件仍按用户自己的宽度不动，只收紧我们的产物（钉 AI-69）。

## v0.1.11（2026-09-15）

> v0.1.10 tag 因发布流水线的产物 glob 事故未产出任何安装包（已修复），本版顺延发布，内容与该 tag 一致并含流水线修复

> 本版是 auto 路由 v2.3（四阶段）+ 全面审查修复的合集；设计全文见 `docs/auto-routing-v2-design.md`（§9 附录记录实现与文档的全部已钉偏差）

- **auto 路由 v2.3 · P0 链级骨架**：逐候选结构化 `chainAttempts`（pickBasis / pickSnapshot / excluded 留痕，日志与响应体双出口）；链预算收口每跳超时；候选评分确定性降序（AR-7）；失败样本限频
- **P1 饱和态（整段避让）**：同一渠道跨 key 连续 429/超时 → 整个候选进饱和态指数退避（60s 起翻倍、30min 封顶、24h 绝对帽），期内不再送流量；饱和将尽（<5s）且预算装得下时给一次探测机会；任一成功即清零。观测面 `/api/auto-health` 出饱和条目（剩余秒数/档位），路由页可一键清除
- **P1.5 视觉三态**：`supportsVision` 三态（true/false/unknown）——导入时按已知多模态家族启发式预填；带图请求 400 学习闭环自动纠偏（≥2 个不同图片指纹才落笔、手动标注上锁不覆盖、可重置）；带图请求对不支持图片的候选软排除（绕行不删粘性）、对 unknown 候选降权 0.25 尽量不付「第一次失败」学费；带图请求的图片 token 计入限额在途预占与窗口过滤（data URL 精算，http URL 取上界常数 1600/张）
- **P2 速度因子 + 粘性慢降级**：流式请求实测吞吐（decode 窗 tok/s，≥16 token 且窗 ≥50ms 才采）折算速度因子——EMA 平滑、1h 半衰期、冷启动（样本 <3）恒 1、floor/cap 可调；统一权重 `weight × 健康分 × 视觉 × 速度因子`，快者多让流量、慢者软降权不剔除。粘性候选首字延迟持续 ≥3× 自身基线 → 临时降粘绕行（绑定保留不续期），恢复后 <2× 迟滞自动回粘；观测面出速度因子/慢降级 chip
- **审查修复轮**（七维团队审查 + 独立复核裁决后收口）：速度因子读侧统一公式——冷启动门根除涓流流量下旧因子被无限期钉死、墙钟回拨 dt 下钳（NTP/休眠恢复曾致 factor 爆到 65）、floor/cap 热改即时重钳；事件侧 EMA 先按样本间隔衰减存量再混合（锯齿根除）；亚毫秒 decode 窗毒样本丢样（曾实测 17tok→17000 tok/s 污染基准）；删除路由/渠道时五态内存（健康/速度/饱和/视觉指纹）聚合回收；流末与链级记账加存活校验（幽灵观测行根除）
- **回滚闭环（审查排期项）**：`autoVision.enabled` 完整闸——软排除/降权随学习闭环一起受闸，关掉即回 v1 行为；设置页新增「auto 路由」卡（饱和/视觉/速度因子三组开关与参数，热改即刻生效）；概览页新增「auto 验收对比」卡——以部署时刻为界前后各 7 天对比三指标（跨候选失败率/全链失败率/p95 首字延迟），G21 门槛（相对降幅 ≥30% 且绝对差 ≥1pp、p95 不劣化）自动判定，含「日志保留 <20 万条会裁掉基线周」提醒；`/api/stats` 支持 from/to 显式窗口
- **文档**：README 与落地页 auto 文案同步 v2 口径；新增 macOS「已损坏，无法打开」的情况说明与解法（Gatekeeper 对未签名应用的隔离误报，`xattr -dr com.apple.quarantine` 一条命令解决，落地页 FAQ 同步）；设计文档新增 §9 实现附注（四阶段落地记录 + 三批已钉偏差）
- **回归**：e2e 312 项 + hardening 191 项全绿（自 v0.1.9 的 211/141 净增 151 项，含速度因子端到端分化钉、采样准入负向钉、饱和/速度状态机时钟注入钉、审查修复回归钉）

## v0.1.9（2026-09-15）

- **使用统计时间口径修正**：原「当天」实为滚动 24 小时窗口，凌晨前会混入昨天数据。新增真正的「今天」（本地零点起自然日）并设为默认，原窗口更名「近 24 小时」诚实化；趋势图分桶联动
- **使用趋势折线图悬停数据浮层**：十字准线吸附时间桶，浮层显示请求数（含失败）、输入 / 输出 / 缓存命中 / 缓存创建与成本，跟随鼠标、贴边翻转、柔和淡入；取代原先逐点的原生 title 提示
- CI：新增 push / PR 触发的 ci.yml（typecheck + 全量测试），回归在提交时暴露，不必等发版

## v0.1.8（2026-09-14）

- **管理台自动刷新软着陆**：概览 30s / 模型路由 15s 的定时刷新不再整页清场闪 loading——数据后台备好后一次性原子替换；内容没变化时零扰动（挂机完全静止）；刷新失败保留当前画面并提示，下一周期自愈；滚动位置保持、输入不抢焦点、内容真变化时 0.5s 柔和淡入
- CI 发布流程加固：softprops 上传加 fail_on_unmatched_files + 发布后资产数硬校验（v0.1.7 曾出现全绿但空 release 的假象）；sha256sums.txt 改由 CI 自动生成

## v0.1.7（2026-09-14）

- **修复：导出/导入配置按钮点击无反应**——使用统计仪表盘的私有作用域块意外包住了配置组与速度排行代码，`async function` 声明没有 Annex B 全局泄漏通道，按钮闭包解析不到函数、同步异常被静默吞掉；已把两段代码搬出块外（`views.overview` 及其图表辅助保留块内），并新增回归钉（e2e §17：vm 全量执行控制台脚本，列 0 顶层函数声明必须全部可解析）
- **检查更新（管理台设置页 + 托盘入口）**：设置页新增「关于与更新」卡，点「检查更新」手动查询 GitHub Releases（10 分钟内存缓存，超时 5s 优雅降级），有新版直接给出当前平台对应的安装包直链；有新版时概览页挂提示条。**纯手动触发，不做启动自动检查**——守住「不联网上报」承诺。托盘菜单新增「检查更新…」，深链直达设置页（Rust 侧零逻辑，版本比较单源在后端）
- 托盘「打开控制台」链路支持视图深链（`&view=settings` 等，换票后一次性消费）

## v0.1.6（2026-09-14）

> ⚠️ 已知问题：本版安装包中「导出全部配置 / 导入配置」按钮点击无反应（见 v0.1.7 修复），请直接升级 v0.1.7

- **速度排行**（新标签页）：按真实模型看 TTFT / 总延迟 / 上游错误率 / 中断率 / failover 率，流式与非流式分表、慢模型自动标橙标红；纯观测，不影响 auto 路由
- **配置组导出/导入**：一键导出全部渠道与路由（不含任何密钥）；导入先预览后确认、按渠道手贴自己的密钥；重复导入幂等，冲突一律上报不覆盖
- 渠道号池为空的行新增琥珀色「待填密钥」提醒，点击直达 key 导入
- **粘性立即生效**：auto 路由行新增按钮（`DELETE /api/routes/:id/sticky`）——调完权重/候选不用等粘性 TTL 过期，一点即按新配比重抽；改名/删除路由时同步清空旧名粘性，不留死绑定
- 实现后全面审查修复轮：导入闸前移（baseUrl/weight/tag/候选数）守 dryRun≡提交、体量三闸、candidatesMerged 回执、待填判据与第三幕直达、导出文案与启发式扩充

另经五路深度复审（前端 / 限流鉴权 / 数据层 / 网关流式 / 壳与CI）加固：

- **抗畸形上游**：CR 行终止的 SSE 流不再整条卡死零输出；超大帧/无边界流在 8MB 上限内有界处置，故障或被恶意的上游再也拖不动网关内存
- **错误信息掩码补全**：上游错误帧的字符串形态、跨行拆分 JSON、对象形态 message 全部统一掩码——此前同一补丁两条路径只改了一半，是否泄漏取决于模型别名设置
- **auto 路由评分修正**：超时击杀不再把同一失败记两笔，健康候选不会被误降权
- **数据安全**：修复正常退出后锁文件残留（极端情况下重启被拒的死角）；数据库读不到时绝不允许空库回写覆盖原文件；旧库畸形 auto 路由不再毒化模型枚举/删除级联
- **管理台交互**：模型页孤儿定时器不再把页面反复拽回；改管理令牌后会话自动跟上（此前需重开控制台）；启停/删除失败即时提示（此前静默如成功）；复制按钮在非 HTTPS 场景可用、掩码串不再误复制；「跟随渠道」协议清除真正生效
- **安全口径统一**：客户端接入示例与 key 明文 reveal 同闸（仅限本机直连）；管理端本机判定收紧（经反代一律视为远程）；停机/票据类敏感端点补限流，限流拒绝也计入日志
- **托盘退出**：优雅停机改确证轮询，退出前落盘窗口内数据不再赌运气
- **发布**：release tag 与包版本不一致直接拒绝出包；本地 desktop:build 自动同步控制台产物

## v0.1.5（2026-09-14）

- 修复 v0.1.4 引入的回归：新增模型时选择路由类型后，后续配置弹窗不再出现

## v0.1.4（2026-09-14）

- 全面审查修复包：渠道 key / 对外 key 明文默认掩码（明文仅限本机直连查看）、日志与计费口径修正、上游流超时改进度时限（正常长流不再被误杀）、管理端与网关限流、错误帧泄漏掩码
- **模型路由与自动路由合并为一张路由表**：统一入口管理单模型与 auto 路由，撞名互斥、候选禁嵌套
- 概览页与仪表盘改版

## v0.1.3（2026-09-04）

- auto 自动路由：候选改为**从模型路由选取**——auto 现在聚合的是路由表中的对外模型名（而非渠道直选），候选健康分、会话粘性等运行时行为不变
- 三平台安装包：macOS（Apple 芯片 / Intel）+ Windows

## v0.1.2（2026-09-04）

- 修复 v0.1.1 引入的回归：macOS arm64 侧车在 hardened runtime 下启动即 SIGTRAP 崩溃
- 发布流程加固：CI 冒烟改为**挂载 dmg 验产物本体**，同类签名问题不再溜进正式包

## v0.1.1（2026-09-04）

- 新增 **macOS（Intel）安装包**：Apple Silicon runner 上交叉产出 x64 dmg，无 Intel 机器也能发 Intel 版
- 说明：v0.1.1 曾因上述签名回归被 v0.1.2 取代，建议直接用 v0.1.2+

## v0.1.0（2026-09-04）

- **首个桌面发行版**：Tauri 2 壳 + SEA 单二进制侧车，一个进程同时提供管理台与统一代理网关
- 菜单栏/托盘常驻：服务自动启动、开机自启、打开数据目录、退出即停服
- 首发平台：macOS（Apple 芯片）、Windows
