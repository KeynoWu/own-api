# 更新说明（Changelog）

所有版本的安装包在 [Releases](../../releases) 页下载；每个版本对应一节说明。

## v0.1.7（2026-09-14）

- **修复：导出/导入配置按钮点击无反应**——使用统计仪表盘的私有作用域块意外包住了配置组与速度排行代码，`async function` 声明没有 Annex B 全局泄漏通道，按钮闭包解析不到函数、同步异常被静默吞掉；已把两段代码搬出块外（`views.overview` 及其图表辅助保留块内），并新增回归钉（e2e §17：vm 全量执行控制台脚本，列 0 顶层函数声明必须全部可解析）
- **检查更新（管理台设置页 + 托盘入口）**：设置页新增「关于与更新」卡，点「检查更新」手动查询 GitHub Releases（10 分钟内存缓存，超时 5s 优雅降级），有新版直接给出当前平台对应的安装包直链；有新版时概览页挂提示条。**纯手动触发，不做启动自动检查**——守住「不联网上报」承诺。托盘菜单新增「检查更新…」，深链直达设置页（Rust 侧零逻辑，版本比较单源在后端）
- 托盘「打开控制台」链路支持视图深链（`&view=settings` 等，换票后一次性消费）

## v0.1.6（2026-09-14）

> ⚠️ 已知问题：本版安装包中「导出全部配置 / 导入配置」按钮点击无反应（见 v0.1.7 修复），请直接升级 v0.1.7

- **速度排行**（新标签页）：按真实模型看 TTFT / 总延迟 / 上游错误率 / 中断率 / failover 率，流式与非流式分表、慢模型自动标橙标红；纯观测，不影响 auto 路由
- **配置组导出/导入**：一键导出全部渠道与路由（不含任何密钥）；导入先预览后确认、按渠道手贴自己的密钥；重复导入幂等，冲突一律上报不覆盖
- 渠道号池为空的行新增琥珀色「待填密钥」提醒，点击直达 key 导入
- **粘性立即生效**：auto 路由行新增按钮（`DELETE /api/routes/:id/sticky`）——调完权重/候选不用等粘性 TTL 过期，一点即按新配比重抽；改名/删除路由时同步清空旧名粘性，不留死绑定
- 实现后全面审查修复轮：导入闸前移（baseUrl/weight/tag/候选数）守 dryRun≡提交、体量三闸、candidatesMerged 回执、待填判据与第三幕直达、导出文案与启发式扩充

另经五路深度复审（前端 / 限流鉴权 / 数据层 / 网关流式 / 壳与CI）加固：

- **抗畸形上游**：CR 行终止的 SSE 流不再整条卡死零输出；超大帧/无边界流在 8MB 上限内有界处置，故障或被恶意的上游再也拖不动网关内存
- **错误信息掩码补全**：上游错误帧的字符串形态、跨行拆分 JSON、对象形态 message 全部统一掩码——此前同一补丁两条路径只改了一半，是否泄漏取决于模型别名设置
- **auto 路由评分修正**：超时击杀不再把同一失败记两笔，健康候选不会被误降权
- **数据安全**：修复正常退出后锁文件残留（极端情况下重启被拒的死角）；数据库读不到时绝不允许空库回写覆盖原文件；旧库畸形 auto 路由不再毒化模型枚举/删除级联
- **管理台交互**：模型页孤儿定时器不再把页面反复拽回；改管理令牌后会话自动跟上（此前需重开控制台）；启停/删除失败即时提示（此前静默如成功）；复制按钮在非 HTTPS 场景可用、掩码串不再误复制；「跟随渠道」协议清除真正生效
- **安全口径统一**：客户端接入示例与 key 明文 reveal 同闸（仅限本机直连）；管理端本机判定收紧（经反代一律视为远程）；停机/票据类敏感端点补限流，限流拒绝也计入日志
- **托盘退出**：优雅停机改确证轮询，退出前落盘窗口内数据不再赌运气
- **发布**：release tag 与包版本不一致直接拒绝出包；本地 desktop:build 自动同步控制台产物

## v0.1.5（2026-09-14）

- 修复 v0.1.4 引入的回归：新增模型时选择路由类型后，后续配置弹窗不再出现

## v0.1.4（2026-09-14）

- 全面审查修复包：渠道 key / 对外 key 明文默认掩码（明文仅限本机直连查看）、日志与计费口径修正、上游流超时改进度时限（正常长流不再被误杀）、管理端与网关限流、错误帧泄漏掩码
- **模型路由与自动路由合并为一张路由表**：统一入口管理单模型与 auto 路由，撞名互斥、候选禁嵌套
- 概览页与仪表盘改版

## v0.1.3（2026-09-04）

- auto 自动路由：候选改为**从模型路由选取**——auto 现在聚合的是路由表中的对外模型名（而非渠道直选），候选健康分、会话粘性等运行时行为不变
- 三平台安装包：macOS（Apple 芯片 / Intel）+ Windows

## v0.1.2（2026-09-04）

- 修复 v0.1.1 引入的回归：macOS arm64 侧车在 hardened runtime 下启动即 SIGTRAP 崩溃
- 发布流程加固：CI 冒烟改为**挂载 dmg 验产物本体**，同类签名问题不再溜进正式包

## v0.1.1（2026-09-04）

- 新增 **macOS（Intel）安装包**：Apple Silicon runner 上交叉产出 x64 dmg，无 Intel 机器也能发 Intel 版
- 说明：v0.1.1 曾因上述签名回归被 v0.1.2 取代，建议直接用 v0.1.2+

## v0.1.0（2026-09-04）

- **首个桌面发行版**：Tauri 2 壳 + SEA 单二进制侧车，一个进程同时提供管理台与统一代理网关
- 菜单栏/托盘常驻：服务自动启动、开机自启、打开数据目录、退出即停服
- 首发平台：macOS（Apple 芯片）、Windows
