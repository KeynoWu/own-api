# 更新说明（Changelog）

所有版本的安装包在 [Releases](../../releases) 页下载；每个版本对应一节说明。

## v0.1.10（2026-09-15）

> 本版是 auto 路由 v2.3（四阶段）+ 全面审查修复的合集；设计全文见 `docs/auto-routing-v2-design.md`（§9 附录记录实现与文档的全部已钉偏差）

- **auto 路由 v2.3 · P0 链级骨架**：逐候选结构化 `chainAttempts`（pickBasis / pickSnapshot / excluded 留痕，日志与响应体双出口）；链预算收口每跳超时；候选评分确定性降序（AR-7）；失败样本限频
- **P1 饱和态（整段避让）**：同一渠道跨 key 连续 429/超时 → 整个候选进饱和态指数退避（60s 起翻倍、30min 封顶、24h 绝对帽），期内不再送流量；饱和将尽（<5s）且预算装得下时给一次探测机会；任一成功即清零。观测面 `/api/auto-health` 出饱和条目（剩余秒数/档位），路由页可一键清除
- **P1.5 视觉三态**：`supportsVision` 三态（true/false/unknown）——导入时按已知多模态家族启发式预填；带图请求 400 学习闭环自动纠偏（≥2 个不同图片指纹才落笔、手动标注上锁不覆盖、可重置）；带图请求对不支持图片的候选软排除（绕行不删粘性）、对 unknown 候选降权 0.25 尽量不付「第一次失败」学费；带图请求的图片 token 计入限额在途预占与窗口过滤（data URL 精算，http URL 取上界常数 1600/张）
- **P2 速度因子 + 粘性慢降级**：流式请求实测吞吐（decode 窗 tok/s，≥16 token 且窗 ≥50ms 才采）折算速度因子——EMA 平滑、1h 半衰期、冷启动（样本 <3）恒 1、floor/cap 可调；统一权重 `weight × 健康分 × 视觉 × 速度因子`，快者多让流量、慢者软降权不剔除。粘性候选首字延迟持续 ≥3× 自身基线 → 临时降粘绕行（绑定保留不续期），恢复后 <2× 迟滞自动回粘；观测面出速度因子/慢降级 chip
- **审查修复轮**（七维团队审查 + 独立复核裁决后收口）：速度因子读侧统一公式——冷启动门根除涓流流量下旧因子被无限期钉死、墙钟回拨 dt 下钳（NTP/休眠恢复曾致 factor 爆到 65）、floor/cap 热改即时重钳；事件侧 EMA 先按样本间隔衰减存量再混合（锯齿根除）；亚毫秒 decode 窗毒样本丢样（曾实测 17tok→17000 tok/s 污染基准）；删除路由/渠道时五态内存（健康/速度/饱和/视觉指纹）聚合回收；流末与链级记账加存活校验（幽灵观测行根除）
- **回滚闭环（审查排期项）**：`autoVision.enabled` 完整闸——软排除/降权随学习闭环一起受闸，关掉即回 v1 行为；设置页新增「auto 路由」卡（饱和/视觉/速度因子三组开关与参数，热改即刻生效）；概览页新增「auto 验收对比」卡——以部署时刻为界前后各 7 天对比三指标（跨候选失败率/全链失败率/p95 首字延迟），G21 门槛（相对降幅 ≥30% 且绝对差 ≥1pp、p95 不劣化）自动判定，含「日志保留 <20 万条会裁掉基线周」提醒；`/api/stats` 支持 from/to 显式窗口
- **文档**：README 与落地页 auto 文案同步 v2 口径；新增 macOS「已损坏，无法打开」的情况说明与解法（Gatekeeper 对未签名应用的隔离误报，`xattr -dr com.apple.quarantine` 一条命令解决，落地页 FAQ 同步）；设计文档新增 §9 实现附注（四阶段落地记录 + 三批已钉偏差）
- **回归**：e2e 312 项 + hardening 191 项全绿（自 v0.1.9 的 211/141 净增 151 项，含速度因子端到端分化钉、采样准入负向钉、饱和/速度状态机时钟注入钉、审查修复回归钉）

## v0.1.9（2026-09-15）

- **使用统计时间口径修正**：原「当天」实为滚动 24 小时窗口，凌晨前会混入昨天数据。新增真正的「今天」（本地零点起自然日）并设为默认，原窗口更名「近 24 小时」诚实化；趋势图分桶联动
- **使用趋势折线图悬停数据浮层**：十字准线吸附时间桶，浮层显示请求数（含失败）、输入 / 输出 / 缓存命中 / 缓存创建与成本，跟随鼠标、贴边翻转、柔和淡入；取代原先逐点的原生 title 提示
- CI：新增 push / PR 触发的 ci.yml（typecheck + 全量测试），回归在提交时暴露，不必等发版

## v0.1.8（2026-09-14）

- **管理台自动刷新软着陆**：概览 30s / 模型路由 15s 的定时刷新不再整页清场闪 loading——数据后台备好后一次性原子替换；内容没变化时零扰动（挂机完全静止）；刷新失败保留当前画面并提示，下一周期自愈；滚动位置保持、输入不抢焦点、内容真变化时 0.5s 柔和淡入
- CI 发布流程加固：softprops 上传加 fail_on_unmatched_files + 发布后资产数硬校验（v0.1.7 曾出现全绿但空 release 的假象）；sha256sums.txt 改由 CI 自动生成

## v0.1.7（2026-09-14）

- **修复：导出/导入配置按钮点击无反应**——使用统计仪表盘的私有作用域块意外包住了配置组与速度排行代码，`async function` 声明没有 Annex B 全局泄漏通道，按钮闭包解析不到函数、同步异常被静默吞掉；已把两段代码搬出块外（`views.overview` 及其图表辅助保留块内），并新增回归钉（e2e §17：vm 全量执行控制台脚本，列 0 顶层函数声明必须全部可解析）
- **检查更新（管理台设置页 + 托盘入口）**：设置页新增「关于与更新」卡，点「检查更新」手动查询 GitHub Releases（10 分钟内存缓存，超时 5s 优雅降级），有新版直接给出当前平台对应的安装包直链；有新版时概览页挂提示条。**纯手动触发，不做启动自动检查**——守住「不联网上报」承诺。托盘菜单新增「检查更新…」，深链直达设置页（Rust 侧零逻辑，版本比较单源在后端）
- 托盘「打开控制台」链路支持视图深链（`&view=settings` 等，换票后一次性消费）

## v0.1.6（2026-09-14）

> ⚠️ 已知问题：本版安装包中「导出全部配置 / 导入配置」按钮点击无反应（见 v0.1.7 修复），请直接升级 v0.1.7

- **速度排行**（新标签页）：按真实模型看 TTFT / 总延迟 / 上游错误率 / 中断率 / failover 率，流式与非流式分表、慢模型自动标橙标红；纯观测，不影响 auto 路由
- **配置组导出/导入**：一键导出全部渠道与路由（不含任何密钥）；导入先预览后确认、按渠道手贴自己的密钥；重复导入幂等，冲突一律上报不覆盖
- 渠道号池为空的行新增琥珀色「待填密钥」提醒，点击直达 key 导入
- **粘性立即生效**：auto 路由行新增按钮（`DELETE /api/routes/:id/sticky`）——调完权重/候选不用等粘性 TTL 过期，一点即按新配比重抽；改名/删除路由时同步清空旧名粘性，不留死绑定
- 实现后全面审查修复轮：导入闸前移（baseUrl/weight/tag/候选数）守 dryRun≡提交、体量三闸、candidatesMerged 回执、待填判据与第三幕直达、导出文案与启发式扩充

另经五路深度复审（前端 / 限流鉴权 / 数据层 / 网关流式 / 壳与CI）加固：

- **抗畸形上游**：CR 行终止的 SSE 流不再整条卡死零输出；超大帧/无边界流在 8MB 上限内有界处置，故障或被恶意的上游再也拖不动网关内存
- **错误信息掩码补全**：上游错误帧的字符串形态、跨行拆分 JSON、对象形态 message 全部统一掩码——此前同一补丁两条路径只改了一半，是否泄漏取决于模型别名设置
- **auto 路由评分修正**：超时击杀不再把同一失败记两笔，健康候选不会被误降权
- **数据安全**：修复正常退出后锁文件残留（极端情况下重启被拒的死角）；数据库读不到时绝不允许空库回写覆盖原文件；旧库畸形 auto 路由不再毒化模型枚举/删除级联
- **管理台交互**：模型页孤儿定时器不再把页面反复拽回；改管理令牌后会话自动跟上（此前需重开控制台）；启停/删除失败即时提示（此前静默如成功）；复制按钮在非 HTTPS 场景可用、掩码串不再误复制；「跟随渠道」协议清除真正生效
- **安全口径统一**：客户端接入示例与 key 明文 reveal 同闸（仅限本机直连）；管理端本机判定收紧（经反代一律视为远程）；停机/票据类敏感端点补限流，限流拒绝也计入日志
- **托盘退出**：优雅停机改确证轮询，退出前落盘窗口内数据不再赌运气
- **发布**：release tag 与包版本不一致直接拒绝出包；本地 desktop:build 自动同步控制台产物

## v0.1.5（2026-09-14）

- 修复 v0.1.4 引入的回归：新增模型时选择路由类型后，后续配置弹窗不再出现

## v0.1.4（2026-09-14）

- 全面审查修复包：渠道 key / 对外 key 明文默认掩码（明文仅限本机直连查看）、日志与计费口径修正、上游流超时改进度时限（正常长流不再被误杀）、管理端与网关限流、错误帧泄漏掩码
- **模型路由与自动路由合并为一张路由表**：统一入口管理单模型与 auto 路由，撞名互斥、候选禁嵌套
- 概览页与仪表盘改版

## v0.1.3（2026-09-04）

- auto 自动路由：候选改为**从模型路由选取**——auto 现在聚合的是路由表中的对外模型名（而非渠道直选），候选健康分、会话粘性等运行时行为不变
- 三平台安装包：macOS（Apple 芯片 / Intel）+ Windows

## v0.1.2（2026-09-04）

- 修复 v0.1.1 引入的回归：macOS arm64 侧车在 hardened runtime 下启动即 SIGTRAP 崩溃
- 发布流程加固：CI 冒烟改为**挂载 dmg 验产物本体**，同类签名问题不再溜进正式包

## v0.1.1（2026-09-04）

- 新增 **macOS（Intel）安装包**：Apple Silicon runner 上交叉产出 x64 dmg，无 Intel 机器也能发 Intel 版
- 说明：v0.1.1 曾因上述签名回归被 v0.1.2 取代，建议直接用 v0.1.2+

## v0.1.0（2026-09-04）

- **首个桌面发行版**：Tauri 2 壳 + SEA 单二进制侧车，一个进程同时提供管理台与统一代理网关
- 菜单栏/托盘常驻：服务自动启动、开机自启、打开数据目录、退出即停服
- 首发平台：macOS（Apple 芯片）、Windows
